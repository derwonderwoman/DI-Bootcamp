import React from "react";

const Hello = (props) => {
    return (
        <> 
            <h1>Hello from {props.name}</h1>
            <h2>Welcome to React</h2>
        </>  
    );
};
export default Hello;