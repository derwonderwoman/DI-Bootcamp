import lodash from "lodash";

export function addition(x,y){
    let sum = lodash.add(x,y);
    return sum;
}

export function multiplication(x,y){
    let sum = lodash.multiply(x,y);
    return sum;
}
